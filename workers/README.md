# Workers config is verplaatst naar apps/api/
# Zie apps/api/wrangler.toml en apps/api/.dev.vars.example
#
# Deze map blijft tijdelijk staan voor lokale referenties; verwijder na
# bevestiging dat secrets en account_id correct in apps/api staan.
