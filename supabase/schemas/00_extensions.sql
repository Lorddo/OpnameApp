-- Fase 0 placeholder. Domeintabellen volgen in fase 1 (declaratief).
-- Schema files are applied in lexicographic order via [db.migrations].schema_paths.

create extension if not exists "pgcrypto" with schema extensions;
